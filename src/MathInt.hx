class MathInt
{
    static public function min(a:Int, b:Int):Int
    {
        return (a < b) ? a : b;
    }

    static public function max(a:Int, b:Int):Int
    {
        return (a > b) ? a : b;
    }
}